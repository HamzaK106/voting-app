<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card">
        <div class="card-header">
          <h3 class="text-center">SMS Verification</h3>
        </div>
        <div class="card-body">
          <p class="text-center mb-4">
            To participate in voting, we need to verify your phone number.
          </p>
          
          <div class="alert alert-info">
            <strong>Phone Number:</strong> <%= current_user.phone %>
          </div>
          
          <div class="text-center">
            <%= link_to "Send Verification Code", request_sms_verification_path, 
                method: :get, 
                class: "btn btn-primary btn-lg",
                data: { confirm: "Send verification code to #{current_user.phone}?" } %>
          </div>
          
          <div class="mt-3 text-center">
            <%= link_to "Change Phone Number", edit_user_registration_path, class: "btn btn-outline-secondary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
